// SPDX-License-Identifier: Apache-2.0
// Modified for Zangetsu on 2026-09-15. See NOTICE.md for upstream credits.
// Generated by build.js; edit common.js and the corresponding *-core.js.
var SOURCE_ID="serenity-manganato", NAME="Manganato", SITE="https://www.natomanga.com", TYPE="manga", VERSION="0.1.0";
// Shared runtime helpers. No Node, DOM, browser cookies or remote code execution.
function text(value) {
  return String(value || '').replace(/<[^>]*>/g, '')
    .replace(/&#x([\da-f]+);/gi, function(_, n) { return String.fromCodePoint(parseInt(n, 16)); })
    .replace(/&#(\d+);/g, function(_, n) { return String.fromCodePoint(+n); })
    .replace(/&quot;/g, '"').replace(/&#39;|&apos;/g, "'").replace(/&nbsp;/g, ' ')
    .replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/&amp;/g, '&').replace(/\s+/g, ' ').trim();
}
function attrs(raw) {
  var out = {}, re = /([^\s=<>/]+)(?:\s*=\s*(?:"([^"]*)"|'([^']*)'|([^\s>]+)))?/g, m;
  while ((m = re.exec(raw))) out[m[1].toLowerCase()] = text(m[2] != null ? m[2] : m[3] != null ? m[3] : m[4] || '');
  return out;
}
// Small non-executing tree parser for the HTML selectors used by these sources.
function html(raw) {
  var root = {tag: '#root', a: {}, children: [], parent: null}, stack = [root];
  var tokens = String(raw).match(/<!--[\s\S]*?-->|<script\b[^>]*>[\s\S]*?<\/script\s*>|<style\b[^>]*>[\s\S]*?<\/style\s*>|<\/?[a-zA-Z][^>]*>|[^<]+/g) || [];
  tokens.forEach(function(t) {
    if (/^<!--|^<(script|style)\b/i.test(t)) return;
    if (/^<\//.test(t)) {
      var end = t.match(/^<\/([^\s>]+)/)[1].toLowerCase();
      for (var i = stack.length - 1; i > 0; i--) if (stack[i].tag === end) { stack.length = i; break; }
      return;
    }
    var parent = stack[stack.length - 1];
    if (t.charAt(0) !== '<') { parent.children.push({tag:'#text', value:text(t), children:[], a:{}, parent:parent}); return; }
    var m = t.match(/^<([^\s/>]+)([\s\S]*?)\/?\s*>$/); if (!m) return;
    var node = {tag:m[1].toLowerCase(), a:attrs(m[2]), children:[], parent:parent}; parent.children.push(node);
    if (!/^(img|input|br|hr|meta|link|source|area|base|embed|param|track|wbr)$/.test(node.tag) && !/\/\s*>$/.test(t)) stack.push(node);
  });
  return root;
}
function all(node, predicate) {
  var out = []; function walk(n) { n.children.forEach(function(c) { if(predicate(c)) out.push(c); walk(c); }); } walk(node); return out;
}
function first(node, predicate) { return all(node, predicate)[0] || null; }
function has(node, cls) { return (' ' + (node.a.class || '') + ' ').indexOf(' ' + cls + ' ') >= 0; }
function content(node) { return node ? (node.tag === '#text' ? node.value : node.children.map(content).join(' ')).replace(/\s+/g,' ').trim() : ''; }
function origin(url) { var m=String(url).match(/^https?:\/\/[^/]+/i); if(!m) throw new Error('Expected HTTP URL'); return m[0]; }
function absolute(url, base) {
  url=text(url); if(/^https?:\/\//i.test(url)) return url;
  if(url.indexOf('//')===0) return 'https:'+url;
  if(!url || /^[a-z][a-z0-9+.-]*:/i.test(url)) throw new Error('Invalid source URL');
  var root=origin(base || SITE), path=url.charAt(0)==='/' ? url : String(base || SITE+'/').split(/[?#]/)[0].replace(/[^/]*$/,'')+url;
  if(/^https?:/.test(path)) path=path.slice(origin(path).length);
  var parts=[]; path.split('/').forEach(function(p){if(p==='..')parts.pop();else if(p!=='.')parts.push(p);});
  return root+parts.join('/');
}
function pathOf(url) { return String(url).replace(/^https?:\/\/[^/]+/i,'').split(/[?#]/)[0]; }
function pageNum(p) { p=Math.floor(Number(p)||1); return Math.max(1,p); }
function headers(ref) { return {Referer:ref || SITE+'/'}; }
async function request(url, ref, json) {
  var h=headers(ref); if(json){h.Accept='application/json, text/javascript, */*; q=0.01';h['X-Requested-With']='XMLHttpRequest';}
  var res=await fetch(url,{headers:h,timeoutMs:12000});
  if(res.status<200 || res.status>=300) throw new Error(NAME+': HTTP '+res.status+' from '+origin(url));
  var body=res.body != null ? res.body : await res.text();
  if(json){try{return JSON.parse(body);}catch(e){throw new Error(NAME+': source returned invalid JSON');}}
  if(/<title>\s*(Just a moment|Attention Required)/i.test(body)) throw new Error(NAME+': open the source in the app WebView to complete its browser check');
  return body;
}
function unique(items, key) { var seen={}; return items.filter(function(x){var k=key(x);if(seen[k])return false;seen[k]=true;return true;}); }
function getInfo() { return {name:NAME,lang:'en',baseUrl:SITE,logo:SITE+'/favicon.ico',type:TYPE,version:VERSION}; }
function item(url,title,cover) { return {id:pathOf(url),title:title,url:url,cover:cover || null,coverHeaders:headers(),type:TYPE,sourceId:SOURCE_ID}; }

// Port of Keiyoushi MangaBox/Manganato (Apache-2.0); see NOTICE.md.
function normalizeQuery(q){return String(q||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/đ/g,'d').replace(/[^a-z0-9]+/g,'_').replace(/^_+|_+$/g,'');}
function parseCatalogue(body){var doc=html(body);return unique(all(doc,function(n){return has(n,'story_item')||has(n,'list-truyen-item-wrap')||has(n,'list-comic-item-wrap');}).map(function(n){var h=first(n,function(x){return x.tag==='h3';}),a=h&&first(h,function(x){return x.tag==='a'&&x.a.href;}),img=first(n,function(x){return x.tag==='img';});return a?item(absolute(a.a.href,SITE+'/'),content(a),img?absolute(img.a['data-src']||img.a.src,SITE+'/'):null):null;}).filter(Boolean),function(x){return x.id;});}
async function search(query,page){return parseCatalogue(await request(SITE+'/search/story/'+encodeURIComponent(normalizeQuery(query))+'?page='+pageNum(page)));}
async function popular(page){return parseCatalogue(await request(SITE+'/manga-list/hot-manga?page='+pageNum(page)));}
async function getHome(){return [{title:'Popular manga',items:await popular(1)},{title:'Latest chapters',items:parseCatalogue(await request(SITE+'/manga-list/latest-manga?page=1'))}];}
function slugOf(url){return pathOf(url).replace(/\/$/,'').split('/').pop();}
async function getDetail(url){url=absolute(url,SITE+'/');var doc=html(await request(url));var area=first(doc,function(n){return has(n,'manga-info-top')||has(n,'panel-story-info');})||doc;var title=first(area,function(n){return n.tag==='h1'||n.tag==='h2';});if(!title)throw new Error('Manganato: title missing; source may have moved');var coverArea=first(doc,function(n){return has(n,'manga-info-pic')||has(n,'info-image');});var img=coverArea&&first(coverArea,function(n){return n.tag==='img';});var desc=first(doc,function(n){return /^(noidungm|panel-story-info-description|contentBox)$/.test(n.a.id||'');});var result=item(url,content(title),img?absolute(img.a.src,url):null);result.description=content(desc);result.status=/completed/i.test(content(area))?'completed':/ongoing/i.test(content(area))?'ongoing':'unknown';result.genres=[];result.studios=[];result.episodes=await getEpisodes(url);return result;}
async function getEpisodes(url){var slug=slugOf(url),data=await request(SITE+'/api/manga/'+encodeURIComponent(slug)+'/chapters?limit=-1',SITE+'/manga/'+slug,true);if(!data.success)throw new Error('Manganato: chapter API did not succeed');return (data.data&&data.data.chapters||[]).filter(function(c){return c.chapter_slug||c.chapterSlug;}).map(function(c){var part=c.chapter_slug||c.chapterSlug;var n=c.chapter_num!=null?c.chapter_num:c.chapterNum;return {id:slug+':'+part,title:c.chapter_name||c.chapterName||'Chapter',number:isFinite(Number(n))?Number(n):null,url:SITE+'/manga/'+encodeURIComponent(slug)+'/'+encodeURIComponent(part),date:c.updated_at||c.updatedAt||null};}).sort(function(a,b){return(a.number||0)-(b.number||0);});}
function literalArray(body,name){var m=body.match(new RegExp('\\b'+name+'\\s*=\\s*(\\[[\\s\\S]*?\\])'));if(!m)return [];try{var a=JSON.parse(m[1]);return Array.isArray(a)?a.filter(function(x){return typeof x==='string';}):[];}catch(e){throw new Error('Manganato: malformed '+name+' list');}}
async function getPages(url){url=absolute(url,SITE+'/');var body=await request(url),images=literalArray(body,'chapterImages'),cdns=literalArray(body,'cdns'),out=[];if(images.length){if(!cdns.length)throw new Error('Manganato: image CDN missing');out=images.map(function(p){return {url:absolute('/'+p.replace(/^\/+/,''),cdns[0]+'/'),headers:headers(url)};});}else{var root=html(body),area=first(root,function(n){return has(n,'container-chapter-reader');});if(area)out=all(area,function(n){return n.tag==='img'&&n.a.src;}).map(function(n){return {url:absolute(n.a.src,url),headers:headers(url)};});}if(!out.length)throw new Error('Manganato: no chapter images found');return out;}
function getChapters(url){return getEpisodes(url);}
